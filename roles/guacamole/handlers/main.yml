---
- name: Cleanup Exploded WARs
  ansible.builtin.file:
    path: "{{ guacamole_tomcat_install_root }}/webapps/{{ item }}"
    state: absent
  loop:
    - ROOT
    - guacamole
    - guacamole.war
- name: Run ldconfig
  ansible.builtin.command:
    cmd: ldconfig
  changed_when: false
- name: Restart Tomcat
  ansible.builtin.service:
    name: tomcat
    state: restarted
    daemon_reload: true
- name: Restart guacd
  ansible.builtin.service:
    name: guacd
    state: restarted
    daemon_reload: true
