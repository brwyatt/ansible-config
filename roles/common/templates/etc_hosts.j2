# Managed by Ansible

# IPv4
{% set hostname = ansible_hostname | lower -%}
{% set fqdn = ansible_fqdn | lower -%}
{% set fqdn = (inventory_hostname | lower) if fqdn == hostname else fqdn -%}
{{ ansible_default_ipv4.address }} {{ fqdn }} {{ hostname }}
127.0.0.1 localhost.localdomain localhost
127.0.0.1 localhost4.localdomain4 localhost4

# IPv6
::1 localhost.localdomain localhost
::1 localhost6.localdomain6 localhost6
